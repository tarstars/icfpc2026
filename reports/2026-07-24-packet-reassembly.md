# Packet Reassembly Ring Candidate

Date: 2026-07-24

Problem: `tcp` / Packet Reassembly

Problem ID: `d61a3af1-c74f-44c0-98f7-4f20eeefa3fb`

Scoring: footprint-tick

## Architecture

The candidate generated by `src/littleman/tcp.py` uses one controller and a
relay joined by a pipe ring. Buffered packets occupy two consecutive ring
values: absolute sequence number followed by packet value.

The controller keeps the next expected sequence number in `B`. For each input
packet it:

1. computes `seq - expected`;
2. copies the delay to `BP` and decrements it 15 times, so a positive result
   detects the required `delay >= 16` loss condition;
3. enqueues `(seq, val)`;
4. waits for all ring values to park, then uses `q` and `]` to count pairs;
5. scans for `seq == expected`, requeueing nonmatches;
6. removes a match, increments `expected`, emits its value, and scans again.

The measured incoming ring pipe has capacity 38, above the worst-case 32
values for 16 buffered packet pairs. Both paths that precede `q` are longer
than a complete ring round trip.

## Local validation

All six public cases pass:

| Case | Ticks |
|---|---:|
| in-order stream | 1,850 |
| single max-displacement swap | 6,380 |
| drain burst | 9,830 |
| loss case | 5,610 |
| shortest stream | 200 |
| block-reversed n=32 | 19,790 |

Average ticks: `7,276.67`

Occupied dimensions: `38 × 41`

Footprint: `1,681`

Local score: `12,232,076.67`

An additional maximum-size adversarial case containing three reverse-ordered
16-packet windows passes in 29,750 ticks. The full repository suite has 67
passing tests.

The generated candidate is 1,561 bytes with SHA-256
`fd8f78f09ce01b1221fb5935deb26808086029791337f5e73fe77060b38420b1`.
It has not been submitted.

## Live context

The problem endpoint reports six public cases and `privateTestCount: 0`, while
the standings use 20 cases. At the snapshot updated
`2026-07-24T17:08:12.051Z`, 19 teams had 20/20; the best full-pass score was
5,602,693.05 and the second-best was 21,294,487.5. The candidate's public
score is therefore promising for a first live attempt, but the 20-case tick
distribution remains unknown until submission.
