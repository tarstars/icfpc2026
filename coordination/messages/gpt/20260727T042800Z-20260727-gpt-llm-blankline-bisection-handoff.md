# handoff: LLM judge-safe row squeeze

- From: gpt
- To: codex
- CC: claude, alexey
- Created UTC: 2026-07-27T04:28:00Z
- Task: `20260727-gpt-llm-blankline-bisection`
- Branch: `agent/gpt-llm-squeeze`
- Payload head: `a1ed9cbadda7532153eaddf3de7dc7be863b7d02`
- Requires acknowledgement: yes

## Exact result

The candidate is generated from accepted `llm_codex_01` by deleting 2,217 of
2,448 globally blank-or-vertical-segment rows. It retains 231 rows whose removal
breaks runtime timing/capacity.

```text
artifact generated by: experiments/gpt-llm-squeeze/build_candidate.py
sha256: 066c00b3f5aa34c0ec9d54c55d6f231bfd429522a9680d3f0cd9b45233d35ed9
bytes: 8,080,520
dimensions: 749×23,580
```

## Score using project documentation

`docs/grading.md` defines score as:

```text
max(width, height)^2 × average ticks across all test cases
```

Measured comparison:

```text
accepted local: max(749,25797)^2 × 12,397,823.285714 = 8,250,568,020,438,638
candidate local: max(749,23580)^2 × 12,314,143.357143 = 6,846,865,658,522,485
reduction: 17.0134%
```

## Public and structural gates

```text
public: 14/14
case ticks:
[5861492, 19855920, 8007221, 10134168, 15231000, 12143224,
 10716618, 7599401, 6937114, 16342436, 11303289, 12942792,
 26946574, 8376758]
rooms/pipes/men: 145/231/143
minimum pipe length: 2
server_compat: pass
alexey_pipecheck: pass
```

All 13,299 pipe instructions retain an identical normalized logical-binding
signature and minimum resolution margin 3:

```text
9f24f98f5d5ec6e25ce19b8d034b97684762209151bfbf9e076f46c2f89a0f70
```

The committed verifier judges each public case in a fresh process to avoid
retaining the large parse/compile heap:

```bash
PYTHONPATH=src python experiments/gpt-llm-squeeze/verify_candidate.py
```

## Negative results

- The equal-footprint alternative using row group 4 instead of group 5 reaches
  tick-cap on most public cases.
- Deleting all 107 candidate columns changes logical pipe bindings and reduces
  the minimum margin from 3 to 1; it was rejected. Columns do not reduce the
  binding dimension while height is 23,580.

## Integration requirements

GPT cannot perform the contest API freshness gate and cannot upload the 8 MB
artifact through the text-only GitHub connector. The exact generator and hash
are committed. Codex should:

1. fetch/reconcile current main and current LLM live state;
2. run the committed verifier;
3. generate the exact bytes and store them through Git LFS under an immutable
   solution name;
4. run preflight and any independent review considered necessary;
5. submit only under the active user authorization and preserve the terminal
   response.

No contest mutation occurred.