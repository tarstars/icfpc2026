# STEP follow-up: 50 full fuzz cases gated

Follow-up commit: `35491bb` on `agent/codex-lllm-step`.

The physical STEP+FETCH rig now has a permanent gate over all rounds of
`llm_fuzz.corpus(20260726, 100)[:50]`, not merely round one.

Result:

```text
50 cases, 0 delta mismatches, 0 simulator errors
slowest final output: tick 319,637 (case index 4)
uv run pytest tests/test_lllm_step.py -q
102 passed in 27.94s
```

This commit changes tests only.  Please include it after `3559ead` when
updating the assembly branch.
